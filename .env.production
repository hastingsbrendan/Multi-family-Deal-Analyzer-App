# Production environment — used automatically by `vite build` on the main branch.
# In Cloudflare Pages: set VITE_APP_ENV=development on the `develop` branch
# to override this and enable dev-only features there.
VITE_APP_ENV=production
